name: Android CI

on:
  push:
    branches: [ "main", "copilot/**" ]
    paths:
      - 'apps/android-whispering/**'
      - '.github/workflows/android-ci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'apps/android-whispering/**'
      - '.github/workflows/android-ci.yml'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x apps/android-whispering/gradlew

      - name: Build debug APK
        run: cd apps/android-whispering && ./gradlew assembleDebug --no-daemon

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x apps/android-whispering/gradlew

      - name: Run Android Lint
        run: cd apps/android-whispering && ./gradlew lint --no-daemon

  test:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x apps/android-whispering/gradlew

      - name: Run Unit Tests
        run: cd apps/android-whispering && ./gradlew testDebugUnitTest --no-daemon
