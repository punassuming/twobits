name: PriceDrop CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'apps/price-drop/**'
      - 'shared/**'
      - 'gradle/libs.versions.toml'
      - '.github/workflows/pricedrop-ci.yml'
      - '.github/workflows/pricedrop-release.yml'
      - '.github/workflows/reusable-validate.yml'
      - '.github/workflows/reusable-build.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'apps/price-drop/**'
      - 'shared/**'
      - 'gradle/libs.versions.toml'
      - '.github/workflows/pricedrop-ci.yml'
      - '.github/workflows/pricedrop-release.yml'
      - '.github/workflows/reusable-validate.yml'
      - '.github/workflows/reusable-build.yml'
  workflow_dispatch:

concurrency:
  group: pricedrop-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate
    uses: ./.github/workflows/reusable-validate.yml
    with:
      changelog_path: apps/price-drop/CHANGELOG.md
      app_root: apps/price-drop
      require_changelog_update: ${{ github.event_name == 'pull_request' || github.ref == 'refs/heads/main' }}
      base_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
      head_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}

  build:
    name: Build & Verify
    needs: validate
    uses: ./.github/workflows/reusable-build.yml
    with:
      app_root: apps/price-drop
      app_name: pricedrop
      gradle_tasks: assembleRelease testDebugUnitTest lintRelease ktlintCheck detekt
    secrets: inherit
