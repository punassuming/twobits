<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- For RecordingForegroundService staying foreground through the transcribing phase after
         recording stops (native/CPU work, no microphone capture) — matches the mediaProcessing
         type declared on that service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <!-- For the on-device model download's foreground worker (SharedModelDownloadWorker) — required
         since Android 14 to justify each foreground service type actually used, matching the
         dataSync type declared where WorkManager promotes that worker (see runLlmDownload). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:name=".ScrybeApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Scrybe">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Scrybe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager routes every setForeground() call (regardless of Worker) through this
             one shared service. Its own manifest declares no foregroundServiceType by default,
             so any Worker requesting a type it doesn't already have crashes with
             "foregroundServiceType ... is not a subset of ..." unless the app's own declaration
             is the union of every type any Worker actually requests: dataSync
             (SharedModelDownloadWorker/runLlmDownload, BackupWorker) and mediaProcessing
             (RetranscribeWorker — the same operation RecordingForegroundService's own
             foregroundServiceType already covers when it runs inline instead of from a retry). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync|mediaProcessing"
            tools:node="merge" />
    </application>

</manifest>
