<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- For the on-device model download's foreground worker (ModelDownloadWorker) — required
         since Android 14 to justify each foreground service type actually used, matching the
         dataSync type declared where WorkManager promotes that worker (see runLlmDownload). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".PriceDropApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PriceDrop">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PriceDrop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notifications.CouponCopyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.twobits.pricedrop.ACTION_COPY_COUPON" />
            </intent-filter>
        </receiver>

        <!-- WorkManager routes every setForeground() call (regardless of Worker) through this
             one shared service. Its own manifest declares no foregroundServiceType by default,
             so any Worker requesting dataSync (see ModelDownloadWorker/runLlmDownload) crashes
             with "foregroundServiceType ... is not a subset of ... 0x00000000" unless the app
             overrides this declaration to actually include it. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
